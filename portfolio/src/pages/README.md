This folder contains simple, beginner-friendly React components:

- `Home.jsx`: a basic landing component.
- `About.jsx`: explains the demo.
- `Contact.jsx`: placeholder for contact info.

Usage:
Import from `src/pages` in `App.jsx` and render with state-based navigation.
